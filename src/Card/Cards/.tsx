
    import React from 'react';

    export default function Of({ width }: { width: number }) {
      return (
        // codegenStart {preset: barrel, import: default, include: ./*.svg }
export {};
// codegenEnd
      );
    }
  